line-length = 100
target-version = "py311"

[lint]
select = ["E", "F", "I", "UP", "B"]

[lint.per-file-ignores]
# Sector notebooks rely on a star import from the core module.
"automatron_build/*" = ["F403", "F405"]
